query ($pageNumber: Int, $pageSize: Int){
    getPromotionList(request: { pageNumber: $pageNumber, pageSize: $pageSize }) {
        data{
            code
            createdDate
            description
            id
            name
            updatedDate
        }
        pageNumber
        pageSize
    }
}