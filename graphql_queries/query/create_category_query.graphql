query($code: String, $name: String) {
    createCategory(request: { code: $code, name: $name }) {
        data {
            code
            createdDate
            id
            name
            updatedDate
        }
    }
}
