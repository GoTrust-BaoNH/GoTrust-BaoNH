query($pageNumber: Int, $pageSize: Int) {
    getFaqList(request: { pageNumber: $pageNumber, pageSize: $pageSize }) {
        data {
            answer
            id
            question
        }
        pageNumber
        pageSize
    }
}
