query($pageNumber: Int, $pageSize: Int) {
    getCategoryList(request: { pageNumber: $pageNumber, pageSize: $pageSize }) {
        data {
            code
            createdDate
            id
            name
            updatedDate
        }
        pageNumber
        pageSize
    }
}