mutation($code: String, $description: String, $name: String) {
    createPromotion(
        request: { code: $code, description: $description, name: $name }
    ) {
        data {
            code
            createdDate
            description
            id
            name
            updatedDate
        }
    }
}
